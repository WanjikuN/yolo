---
- name: Yolo Playbook
  hosts: all
  become: yes

  vars_files:
    - vars.yml

  pre_tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
  tasks:
    - name: Install git
      include_role:
        name: git
       

    